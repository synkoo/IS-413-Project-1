@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model AvailableTimesViewModel

@*Display available times by day of week*@

<div class="container">
    <div class="row">
        <div class="col">
            @foreach (var x in Model.Monday)
            {
                <form asp-action="AvailableTimes" method="post">
                    <input type="hidden" asp-for="@x.DayOfWeek" name="dayOfWeek" />
                    <input type="hidden" asp-for="@x.HourBlock" name="hourBlock" />
                    <input type="hidden" asp-for="@x.TimeId" name="timeId" />
                    <input type="hidden" asp-for="@x.IsBooked" name="isBooked" />
                    <button class="btn-success btn-sm custom" type="submit">@x.DayOfWeek @x.HourBlock</button>
                    <br /><br />
                </form>
            }
        </div>
        <div class="col">
            @foreach (var x in Model.Tuesday)
            {
                <form asp-action="AvailableTimes" method="post">
                    <input type="hidden" asp-for="@x.DayOfWeek" name="dayOfWeek" />
                    <input type="hidden" asp-for="@x.HourBlock" name="hourBlock" />
                    <input type="hidden" asp-for="@x.TimeId" name="timeId" />
                    <input type="hidden" asp-for="@x.IsBooked" name="isBooked" />
                    <button class="btn-success btn-sm custom" type="submit">@x.DayOfWeek @x.HourBlock</button>
                    <br /><br />
                </form>
            }
        </div>
        <div class="col">
            @foreach (var x in Model.Wednesday)
            {
                <form asp-action="AvailableTimes" method="post">
                    <input type="hidden" asp-for="@x.DayOfWeek" name="dayOfWeek" />
                    <input type="hidden" asp-for="@x.HourBlock" name="hourBlock" />
                    <input type="hidden" asp-for="@x.TimeId" name="timeId" />
                    <input type="hidden" asp-for="@x.IsBooked" name="isBooked" />
                    <button class="btn-success btn-sm custom" type="submit">@x.DayOfWeek @x.HourBlock</button>
                    <br /><br />
                </form>
            }
        </div>
        <div class="col">
            @foreach (var x in Model.Thursday)
            {
                <form asp-action="AvailableTimes" method="post">
                    <input type="hidden" asp-for="@x.DayOfWeek" name="dayOfWeek" />
                    <input type="hidden" asp-for="@x.HourBlock" name="hourBlock" />
                    <input type="hidden" asp-for="@x.TimeId" name="timeId" />
                    <input type="hidden" asp-for="@x.IsBooked" name="isBooked" />
                    <button class="btn-success btn-sm custom" type="submit">@x.DayOfWeek @x.HourBlock</button>
                    <br /><br />
                </form>
            }
        </div>
        <div class="col">
            @foreach (var x in Model.Friday)
            {
                <form asp-action="AvailableTimes" method="post">
                    <input type="hidden" asp-for="@x.DayOfWeek" name="dayOfWeek" />
                    <input type="hidden" asp-for="@x.HourBlock" name="hourBlock" />
                    <input type="hidden" asp-for="@x.TimeId" name="timeId" />
                    <input type="hidden" asp-for="@x.IsBooked" name="isBooked" />
                    <button class="btn-success btn-sm custom" type="submit">@x.DayOfWeek @x.HourBlock</button>
                    <br /><br />
                </form>
            }
        </div>
        <div class="col">
            @foreach (var x in Model.Saturday)
            {
                <form asp-action="AvailableTimes" method="post">
                    <input type="hidden" asp-for="@x.DayOfWeek" name="dayOfWeek" />
                    <input type="hidden" asp-for="@x.HourBlock" name="hourBlock" />
                    <input type="hidden" asp-for="@x.TimeId" name="timeId" />
                    <input type="hidden" asp-for="@x.IsBooked" name="isBooked" />
                    <button class="btn-success btn-sm custom" type="submit">@x.DayOfWeek @x.HourBlock</button>
                    <br /><br />
                </form>
            }
        </div>
        <div class="col">
            @foreach (var x in Model.Sunday)
            {
                <form asp-action="AvailableTimes" method="post">
                    <input type="hidden" asp-for="@x.DayOfWeek" name="dayOfWeek" />
                    <input type="hidden" asp-for="@x.HourBlock" name="hourBlock" />
                    <input type="hidden" asp-for="@x.TimeId" name="timeId" />
                    <input type="hidden" asp-for="@x.IsBooked" name="IsBooked" />
                    <button class="btn-success btn-sm custom" type="submit">@x.DayOfWeek @x.HourBlock</button>
                    <br /><br />
                </form>
            }
        </div>
    </div>
</div>